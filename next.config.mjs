/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.unsplash.com", "lh3.googleusercontent.com", "img.clerk.com","api.dicebear.com"],
  },
};

export default nextConfig;
